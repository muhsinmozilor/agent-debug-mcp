# Copied to .env (git-ignored). Used by scripts/publish-relay.sh (pnpm release:relay).
# npm granular access token with read+write on packages (or an automation token). Never commit the real one.
NPM_TOKEN=
