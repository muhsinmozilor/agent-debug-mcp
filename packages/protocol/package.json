{
  "name": "@devtools-mcp/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Wire protocol, tool contract, tagged serialisation and cursors shared by the Agent Debug MCP extension and relay",
  "exports": {
    ".": { "types": "./src/index.ts", "default": "./src/index.ts" }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vitest": "^4.1.11"
  }
}
